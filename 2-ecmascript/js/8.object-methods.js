const superhero = {
    id: 1,
    name: 'peter',
    app: 'parker',
    alias: 'spiderman'
};

console.log('Object.keys:', Object.keys(superhero));
console.log('Object.values:', Object.values(superhero));
console.log('Object.entries:', Object.entries(superhero));
console.log('Object.length:', Object.keys(superhero).length);